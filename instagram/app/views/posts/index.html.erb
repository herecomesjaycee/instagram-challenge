<% if @posts.any? %>
  <% @posts.each do |post| %>
    <h2> <%= post.caption %> </h2>
    <%= image_tag post.image.url(:medium) %>
  <% end %>
<% else %>
  No posts yet
<% end %>
<a href='posts/new'>Add a post</a>